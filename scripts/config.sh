#!/usr/bin/env bash

# TODO Place in a script file that can be loaded later to reduce reptition
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(cd "$SCRIPTS_DIR/.." && pwd)"

# TODO Configuration step via CMake
